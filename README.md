
  # High-Fidelity UI Design

  This is a code bundle for High-Fidelity UI Design. The original project is available at https://www.figma.com/design/hmPpYPzLcoO6BnEBw5FXJU/High-Fidelity-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  